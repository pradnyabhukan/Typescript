// allows us to define a set of named constants
// we can assign string or numeric values to them

enum orderStatus {
    PENDING,
    SHIPPED,
    DELIVERED,
    RETURED
}

enum Directions {
    EAST = 5,
    WEST,
    NORTH,
    SOUTH
}

enum ArrowKeys {
    UP = "up",
    DOWN = "down",
    RIGHT = "right",
    LEFT = "left"
}